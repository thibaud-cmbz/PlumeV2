[project]
name = "plume-discovery"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["plume-core"]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.sources]
plume-core = { workspace = true }
