[project]
name = "plume-core"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28", "pydantic-settings>=2.6", "tenacity>=9.0"]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
